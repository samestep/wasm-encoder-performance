[package]
name = "wasm-encoder-performance"
publish = false
edition = "2021"

[dependencies]
regex = "1"
wasm-encoder = "0.223"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "comparison"
harness = false
